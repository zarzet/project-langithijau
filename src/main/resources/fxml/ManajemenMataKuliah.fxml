<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.studyplanner.kontroler.KontrolerManajemenMataKuliah"
            prefWidth="1000" prefHeight="700">

    <center>
        <SplitPane dividerPositions="0.3" styleClass="split-pane-custom">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <!-- Panel Kiri: Mata Kuliah -->
            <VBox spacing="15">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>

                <Label text="Daftar Mata Kuliah" styleClass="section-title"/>

                <TableView fx:id="courseTable" VBox.vgrow="ALWAYS" styleClass="table-view">
                    <columns>
                        <TableColumn fx:id="courseCodeColumn" text="Kode" minWidth="60" prefWidth="80"/>
                        <TableColumn fx:id="courseNameColumn" text="Nama Mata Kuliah" minWidth="120"/>
                    </columns>
                </TableView>

                <HBox spacing="15" alignment="CENTER_LEFT">
                    <padding>
                        <Insets top="5" right="0" bottom="0" left="0"/>
                    </padding>
                    <Button fx:id="addCourseBtn" text="Tambah" styleClass="btn-success"/>
                    <Button fx:id="editCourseBtn" text="Edit" styleClass="btn-primary"/>
                    <Button fx:id="deleteCourseBtn" text="Hapus" styleClass="btn-danger"/>
                </HBox>
            </VBox>

            <!-- Panel Kanan: Topik dan Ujian -->
            <VBox spacing="25">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>

                <!-- Bagian Topik -->
                <VBox spacing="15">
                    <Label text="Topik Materi" styleClass="section-title"/>

                    <TableView fx:id="topicTable" prefHeight="250" styleClass="table-view">
                        <columns>
                            <TableColumn fx:id="topicNameColumn" text="Nama Topik" minWidth="150"/>
                            <TableColumn fx:id="topicPriorityColumn" text="Prioritas" minWidth="70" prefWidth="90"/>
                            <TableColumn fx:id="topicDifficultyColumn" text="Kesulitan" minWidth="70" prefWidth="90"/>
                            <TableColumn fx:id="topicReviewCountColumn" text="Review" minWidth="60" prefWidth="80"/>
                        </columns>
                    </TableView>

                    <HBox spacing="15" alignment="CENTER_LEFT">
                        <padding>
                            <Insets top="5" right="0" bottom="0" left="0"/>
                        </padding>
                        <Button fx:id="addTopicBtn" text="Tambah" styleClass="btn-success"/>
                        <Button fx:id="editTopicBtn" text="Edit" styleClass="btn-primary"/>
                        <Button fx:id="deleteTopicBtn" text="Hapus" styleClass="btn-danger"/>
                    </HBox>
                </VBox>

                <Separator styleClass="separator"/>

                <!-- Bagian Ujian -->
                <VBox spacing="15">
                    <Label text="Jadwal Ujian" styleClass="section-title"/>

                    <TableView fx:id="examTable" prefHeight="200" styleClass="table-view">
                        <columns>
                            <TableColumn fx:id="examTitleColumn" text="Judul" minWidth="120"/>
                            <TableColumn fx:id="examTypeColumn" text="Tipe" minWidth="80" prefWidth="100"/>
                            <TableColumn fx:id="examDateColumn" text="Tanggal" minWidth="100" prefWidth="120"/>
                        </columns>
                    </TableView>

                    <HBox spacing="15" alignment="CENTER_LEFT">
                        <padding>
                            <Insets top="5" right="0" bottom="0" left="0"/>
                        </padding>
                        <Button fx:id="addExamBtn" text="Tambah" styleClass="btn-success"/>
                        <Button fx:id="editExamBtn" text="Edit" styleClass="btn-primary"/>
                        <Button fx:id="deleteExamBtn" text="Hapus" styleClass="btn-danger"/>
                    </HBox>
                </VBox>

            </VBox>

        </SplitPane>
    </center>

</BorderPane>
